package com.dfs.corporate.domain;

/** Corporate portal workflow / admin roles (in addition to Account.role PARTY_USER). */
public enum PortalRole {
    PARTY_ADMIN,
    MAKER,
    CHECKER,
    APPROVER,
    RELEASER
}
