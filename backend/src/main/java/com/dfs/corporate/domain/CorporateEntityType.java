package com.dfs.corporate.domain;

/**
 * Annex-C entity types in scope for DFS Corporate (1–4 only).
 */
public enum CorporateEntityType {
    SOLE_PROPRIETORSHIP,
    SMALL_BUSINESS,
    PARTNERSHIP,
    LLP
}
